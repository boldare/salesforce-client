{
    "name": "xsolve-pl/salesforce-client",
    "license": "MIT",
    "type": "library",
    "require": {
        "php": ">=7.0.0",
        "jms/serializer": "~1.4",
        "psr/http-message": "~1.0"
    },
    "require-dev": {
        "phpunit/phpunit": "5.5.*"
    },
    "suggest": {
        "guzzlehttp/guzzle": "Needed for sending HTTP requests using GuzzleClient",
        "blablacar/redis-client": "Needed if Redis will be used as the storage for tokens"
    },
    "autoload":{
        "psr-4":{
            "Xsolve\\SalesforceClient\\": ""
        }
    }
}
